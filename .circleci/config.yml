# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2
jobs:
  build:
    docker:
      - image: cimg/go:1.13
    working_directory: /go/src/github.com/filipenos/projects
    steps:
      - checkout
      - run: go mod download
      - run: go build